#include "main.h"
/**
 * add - a fun.. that adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: the sum of two integer
 */
int add(int a, int b)
{
	return (a + b);
}
